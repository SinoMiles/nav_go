version: '3.8'

services:
  NavGo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: NavGo-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://NavGo:3afnijdxHaMrJHsT@39.98.161.189:27017/NavGo
      - NEXTAUTH_SECRET=your-nextauth-secret-key-change-this-in-production
      - NEXTAUTH_URL=http://localhost:3000
      - JWT_SECRET=your-jwt-secret-key-change-this-in-production
    volumes:
      - ./uploads:/app/uploads
      - ./public/themes:/app/public/themes
    networks:
      - NavGo-network

networks:
  NavGo-network:
    driver: bridge
